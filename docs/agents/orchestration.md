# Orchestration

Main Hermes owns intent, recall, task graph, and final synthesis. Coding agents own bounded implementation after inspecting the repo. Reviewers should verify spec compliance first, then code quality.

For dependent work, use stacked branches/PRs and document base/dependency notes clearly.
